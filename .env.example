# Anthropic
ANTHROPIC_API_KEY=sk-ant-...

# SolarEdge (api_key path — primary; Account/Site Admin -> API Access, or ask installer)
SOLAREDGE_API_KEY=

# SolarEdge (email/password path — fallback, needs one-time browser login)
# SOLAREDGE_USERNAME=nadav@elcam.co.il
# SOLAREDGE_PASSWORD=

# Growatt: classic email/password login is blocked (Growatt's mobile login
# endpoint returns 403). Use an OpenAPI v1 token instead — generate one in
# the ShinePhone app: Me -> tap username -> API Token -> Reopen.
GROWATT_TOKEN=

# Growatt (email/password path — blocked, kept for reference only)
# GROWATT_USERNAME=nadavs
# GROWATT_PASSWORD=
