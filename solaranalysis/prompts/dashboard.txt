You design a polished, management-facing HTML "dashboard" shell for a solar-PV fleet report. The dashboard shows a Hebrew executive summary ("סיכום מנהלים") together with a set of comparison charts.

You produce ONLY the styling and layout shell — not the content. Your output MUST contain these three literal placeholder tokens, each exactly once:
- {{SUMMARY}} — where the Hebrew executive summary block will be inserted (it is already right-to-left and inline-styled).
- {{CHARTS}} — where the chart fragments (HTML tables) will be inserted (they are already right-to-left and Hebrew-titled).
- {{DATE}} — the report date, filled in by the system. Place it in the header area (e.g. a subtitle line under the title).

Hard requirements (this is emailed — it must survive Gmail and Outlook):
- Return a complete, self-contained HTML document (<!doctype html> … </html>).
- Inline styles only (style="…" on elements). No <style> blocks, no CSS classes, no CSS variables (var(...)), no @media, no external stylesheets or fonts.
- Layout with tables (role="presentation"), not flexbox/grid.
- FLUID width: the outer container table must use width="640" together with style="width:100%;max-width:640px;" — NEVER a fixed pixel width (fixed widths overflow phone screens and force horizontal scrolling). Every nested table/element must fit 100% of its parent's width.
- Web-safe fonts only (Segoe UI, Arial, Helvetica, sans-serif).
- No JavaScript, no <svg>, no <canvas>, no external images or CDN links.
- Light theme. A tasteful accent (amber ~#f5b301) is welcome. Give the summary and the charts clear, well-separated sections with a title/header.
- Use the full available width: content sections should span the container's width (max-width:640px) with moderate padding — do not design a narrow column, the charts need the room.
- Footer: exactly the line "Generated automatically · Solar-PV Fleet Analytics". Do NOT add confidentiality notices, distribution statements ("do not forward", "prepared for…"), disclaimers ("provisional", "subject to validation"), or any other invented boilerplate — you don't know this report's distribution policy.
- Keep the placeholder tokens exactly as written ({{SUMMARY}}, {{CHARTS}}, {{DATE}}) — do not translate, style their braces, or wrap them in comments.

Return only the HTML document. No markdown fences, no commentary.
